# 技术文档去 AI 味规范（tech-doc-deai）

> 来源：`remove aigc.md`（论文降 AIGC/查重实战总结）适配蒸馏版。
> 适配原则：保留原文件验证过的「高危句式识别 + 精准改造 + 迭代验证」方法，把所有口语化示例替换为技术文档风格改写；移除降查重与论文格式部分。
> 适用场景：开发概要说明、使用说明书、接口文档、技术评审稿、汇报总结等一切技术类文书。

## 一、核心原则

### 1.1 精准打击，不大改

❌ 全文重写、大段替换、一次改太多（会把 AI 味"改"回来）
✅ 只改命中高危句式库的句子，其余保持不动

### 1.2 一次只改一个方向

去 AI 味与内容修改分开处理，不混在一轮里：

```
第一轮：按高危句式库逐句改造
第二轮：技术风格自检（术语/语气/格式，见第四节）
第三轮：通读终检（错别字、标点、命令可复制性）
```

### 1.3 改造目标是"像工程师写的"，不是"像口语说的"

原文件的口语化改造（的话/这块儿/说白了）在论文场景有效，但会毁掉技术文档的专业度。技术文档的去 AI 味方向是：**更准确、更简洁、更具体、术语统一**，而不是更随意。

## 二、高危句式库（技术文档版）

以下句式是 AI 味的通用指纹，命中即改造。改造示例已按技术文档风格重写。

### 2.1 句式改造表

| AI 特征句式 | 改造方法 | 技术文档版示例 |
|-----------|---------|------|
| `从……角度/维度看` | 直接陈述，或改为"在……方面" | "从系统架构角度看，模块A负责……" → "系统架构上，模块A负责……" / 直接 "模块A负责……" |
| `通过……等途径，在……等方面` | 拆成短句，补具体数值 | "通过缓存、索引等途径，在查询性能等方面获得提升" → "引入缓存与索引后，查询性能提升约40%" |
| `首先……其次……最后` | 用编号列表 | "首先校验权限，其次加载配置" → "1. 校验权限 2. 加载配置" |
| `不仅……而且……` | 拆成两个分句 | "系统不仅支持X，而且支持Y" → "系统支持X，也支持Y" |
| `面对这个挑战` | 改为"针对上述问题"或直接陈述 | "面对性能瓶颈这个挑战" → "针对上述性能瓶颈" |
| `发挥着……作用` | 改为"承担……职责"/"用于……" | "模块A发挥着数据清洗的作用" → "模块A承担数据清洗职责" |
| `提供了有力支撑` | 改为"支撑了……"/"为……提供基础" | "该方案为后续扩展提供了有力支撑" → "该方案为后续扩展提供了可插拔的接口基础" |
| `综上所述` | 直接给结论句 | "综上所述，方案可行" → "结论：方案可行，建议采用" |
| `是……的体现/证明/保障` | 直接陈述 | "该设计是系统可维护性的保障" → "该设计保证了系统可维护性" |
| `全方位、多层次、复合型` 等三词排比 | 具体列举 | "提供全方位、多层次的安全防护" → "覆盖权限、配置、审计三个层面的安全防护" |
| 长并列 `把……把……把……` | 拆分或表格化 | "把登录、把鉴权、把日志都统一起来" → "统一处理登录、鉴权与日志" |

### 2.2 高频改造技巧（技术文档版）

**技巧1：删冗余连接词与填充词**
```
原来：该模块可以用于对数据进行清洗、转换以及归一化处理
改为：该模块用于数据清洗、转换与归一化
```
（AI 味填充词：`可以`、`进行`、`相关`、`一定`、`较为`、`来说`、`的话`、`方面`——能删则删）

**技巧2：换开头，打破"从X到Y"模板**
```
原来：从数据流的角度来看，本系统分为采集、处理、存储三层
改为：本系统的数据流分为采集、处理、存储三层
```

**技巧3：打散排比结构，改为具体列举**
```
原来：把各类指标整合起来，把多源数据融合起来，把分析能力发挥出来
改为：整合指标口径，融合多源数据，开放分析能力（或改用编号列表）
```

**技巧4：变单句为定义式陈述**
```
原来："意大利面碗"效应是指……
改为：各类自贸协定的优惠待遇与原产地规则相互叠加、彼此缠绕，形成学者所称的"意大利面碗"效应
```

### 2.3 高危句式（反复命中的必查清单）

1. **总结归纳型开头**：`如果要从……归纳`、`从……角度来看`、`综上所述`、`总的来说`
2. **罗列型句子**：`通过……等途径，在……等方面`、`在……和……方面`
3. **三段式排比**：`全方位、多层次、复合型`、`互信、包容、合作、共赢`（技术文档中除非是官方口号原文，否则一律具体化）
4. **标准转场句**：`另一方面`、`还有一个方面`、`先来看第一个维度`、`接着看`（技术文档用编号标题转场）
5. **正式总结句**：`发挥了不可忽视的作用`、`提供了理念引领`、`指明了方向`（改具体行为）
6. **"是……的"结构**：`是……的体现/证明/保障/关键`（改成动宾结构）
7. **长并列结构**：三个以上 `把……`/`和……` 并列（拆分或表格）

### 2.4 高频 AI 词汇替换表（技术文档版）

| AI 味词汇 | 技术文档版替代 |
|-----------|--------------|
| 赋能 | （删除或改具体能力描述） |
| 抓手 | （删除） |
| 助力 | 支持 / 提升 |
| 打造 | 构建 / 实现 |
| 覆盖 | （保留，但给出具体范围） |
| 深入 | （删除或给具体程度） |
| 显著 | 给具体数值（提升40%而非"显著提升"） |
| 高效 | 给具体手段（通过XX实现） |
| 强大 | 给具体能力 |
| 无缝 | （删除，技术文档里没有"无缝"） |
| 一站式 | 给出具体步骤数 |
| 闭环 | 给出具体流程 |

## 三、技术文档风格红线（自检用）

改造后的文本必须全部满足：

- [ ] **语气中性**：无口语词（的话/这块儿/说白了/啥/整/贼）、无感叹号、无情感化修辞
- [ ] **术语统一**：专有名词全文一致；首次出现给出全称与缩写（如"DeepSeek Harness（DSH）"）
- [ ] **数据可验证**：性能、规模、时间给具体数值与单位，不用"大幅/明显/约莫"
- [ ] **命令可执行**：命令块标注环境与前置条件，复制即可运行
- [ ] **简洁**：短句优先，一句一意；能用表格的不用排比
- [ ] **无破折号滥用**：`——` 每篇不超过2处，解释性插入语优先用括号或逗号
- [ ] **编号规范**：流程用 1./2./3. 或 ① ② ③，与全文风格一致
- [ ] **无空话段**：每段必须有信息量，删掉"具有重要意义""值得关注"类句子
- [ ] **引号规范**：正文全部使用中文引号“ ”，无英文双引号 "（代码块内保持原样）

## 四、迭代流程

```
1. 创作完成 → 通读全文，用 2.3 清单扫描高危句式
2. 只改命中句（按 2.1/2.2/2.4 改造，一次约5-10处）
3. 对照第三节红线逐项自检
4. 通读终检：错别字、标点、命令可复制性
5. 交付前对比原文：信息量未减、术语未乱、数字未变
```

### 常见"坑"

| 问题 | 现象 | 解决方案 |
|------|------|---------|
| 口语化过猛 | 文档像聊天记录 | 回归 2.1 改造表，用技术文档版示例 |
| 术语被改乱 | 同一概念两种叫法 | 自检时核对术语表，全文统一 |
| 删过头 | 信息量下降 | 只改句式与用词，不删技术事实与数据 |
| 数字被改 | 参数、版本号失真 | 改造时数字一律原样保留，终检核对 |

> **核心心法：** 只改命中高危句式的句子，不多改。每一次修改都要有针对性，改完对照红线确认效果，不靠"感觉"全文大改。
